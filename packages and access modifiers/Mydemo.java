package p2;
public class Mydemo {
int i=0; // default can be accessed only in same package
public int a=1; //can be accessed anywhere
protected int b=2; //can be accessed in same package and also in different package provided there must be a subclass
private int c=3;
}
